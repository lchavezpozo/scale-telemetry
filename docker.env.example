# Configuración Docker para Scale Telemetry
# Copia este archivo a .env y ajusta los valores según tu entorno

# ============================================================================
# MQTT - Configuración para EMQX
# ============================================================================
# Si EMQX está en el host, usa:
#   - Con network_mode: host -> MQTT_BROKER=localhost
#   - Con network bridge -> MQTT_BROKER=host.docker.internal (macOS/Windows) 
#                           MQTT_BROKER=172.17.0.1 (Linux)
# Si EMQX está en otro contenedor -> MQTT_BROKER=nombre_contenedor_emqx

MQTT_BROKER=localhost
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
# Habilitar SSL/TLS para usar wss:// en lugar de ws://
# Si es true, el puerto típico de EMQX WSS es 8084
MQTT_USE_SSL=false

# ============================================================================
# Logs
# ============================================================================
# Directorio donde se guardan los logs (montado como volumen en Docker)
LOG_DIR=/var/log/scale-telemetry

# ============================================================================
# Dispositivos
# ============================================================================
# Los dispositivos se configuran en devices.json (ver devices.json.example)
DEVICES_CONFIG_PATH=devices.json

# ============================================================================
# Simulador (solo para docker-compose.dev.yml)
# ============================================================================
SIMULATOR_RANDOM=false
SIMULATOR_WEIGHT=120

